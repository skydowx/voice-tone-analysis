# Isolated research-only dependencies. Install into a separate virtualenv; these
# are intentionally excluded from the production image.
torch==2.11.0
torchaudio==2.11.0
funasr==1.4.1
huggingface-hub==1.4.1
soundfile==0.13.1
